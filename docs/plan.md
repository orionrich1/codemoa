# CodeMoa 기획 통합 문서

**구성**: 전역 로드맵(Part A) · 메인 페이지(Part B) · AI 코딩 문제(Part C) · **공통 백로그(Part D)**.  
**도메인별 단일 파일**: `community-spec.md`, `information-spec.md`, `employment-team-spec.md`.  
**항상 참고**: `HARNESS_GUIDE.md`, `DESIGN.md`.

---

## Part D. 공통 백로그 (후순위·선택 과제)

> 하네스 **기획서 수명 주기**: 필수 스코프는 각 `*-spec.md`에서 `[x]`로 정리하고, **착수하지 않은 항목은 본 Part에만** 둔다. 도메인 명세 본문에는 동일 `[ ]`를 반복하지 않는다. 착수 시 여기서 항목을 옮겨 해당 spec에 수락 조건을 다시 쓴다.

| 출처 | 항목 | 비고 |
|------|------|------|
| 커뮤니티 | 긴 본문·댓글 **앵커 링크**(목차·댓글 영역 이동) | 선택 UX |
| 커뮤니티 | 작성·수정 **임시 저장**(로컬 스토리지) 또는 나가기 전 확인 | 선택 |
| IT 정보 | 강좌·도서·공모전 목록 **3 HTML → fragments / 단일 템플릿 + `th:switch`** | 유지보수 |
| IT 정보 | 목록 **그리드·리스트 전환 토글** 또는 소형 화면 **세로 스택** 사용자 선택 | UX 선택 |
| IT 정보 | 도서 목록·AJAX에 **writer/content 등 검색 타입** 선택 UI (매퍼는 지원) | UI 정합 |
| IT 정보 | 허브 **통합 검색**(키워드 한 번에 세 영역) | 후순위 |
| IT 정보 | (선택) 공모전 **일괄 마감·상태 자동 전환 배치** | 운영 |
| IT 정보 | (선택) **스크랩/찜** | 확장 |
| 채용·팀 | **채용·팀 허브** (`/jobs`·`/career` 등) 카드 2개 | IA |
| 채용·팀 | 헤더 드롭다운 **허브 1링크 vs 2링크** 결정 | IA |
| 채용·팀 | 채용 목록 **지역·경력** 필터 (데이터·API 가능 시) | 데이터 의존 |
| 채용·팀 | 채용 **사이트 내 상세** `GET /employmentList/{id}` + 원문 보기 | 선택 |
| 채용·팀 | **메뉴·버튼 URL 매핑 표** 명세 1회 정리 | 문서 |
| 채용·팀 | 팀 모집 **필수 필드 검증** 서버·클라 메시지 완전 일치 | 품질 |
| 채용·팀 | 팀 모집 첨부 **용량·확장자 서버 검증**과 폼 안내 일치 | 보안·품질 |

---

## Part A. 전역 PRD · 우선순위 로드맵

## 1) 목표
- 사용자 관점에서 "탐색이 쉽고", "핵심 기능 진입이 빠르며", "화면/동작이 일관된" 서비스로 개선한다.
- 운영 관점에서 화면 템플릿, 스타일, 라우팅, 예외 처리 기준을 정리해 유지보수 비용을 낮춘다.
- 이번 문서는 **구현 전 기획/설계 기준**이며, 우선순위 높은 작업부터 순차 진행한다.

## 2) 현재 구조 요약 (확인 기준)
- 서버: Spring Boot + Thymeleaf + MyBatis
- 화면 공통: `src/main/resources/templates/layouts/default_layout.html`, `src/main/resources/templates/fragments/*`
- 스타일 공통: `src/main/resources/static/css/common.css`, `src/main/resources/static/css/main.css`
- 메인 진입: `src/main/java/com/codemoa/project/domain/main/controller/MainController.java`, `src/main/resources/templates/views/main/index.html`
- 도메인 화면: `src/main/resources/templates/views/**`
- 도메인 컨트롤러: `src/main/java/com/codemoa/project/domain/**/controller/*Controller.java`

## 3) 핵심 문제 정의
- 네비게이션/랜딩/검색의 UX는 존재하지만, 도메인별 진입 정책과 화면 품질이 일정하지 않다.
- URL/엔드포인트 네이밍 규칙이 일부 불일치(대소문자 혼용, 복수 경로 공존)하여 사용자/개발자 혼란 가능성이 있다.
- 예외 처리와 에러 UX가 API 중심으로 구성되어 웹 화면 사용자 경험(에러 페이지, 안내 문구)이 약하다.
- 공통 디자인 토큰은 잘 정리되어 있으나, 개별 페이지 인라인 스타일/페이지별 편차를 줄일 필요가 있다.
- 운영/배포 관점에서 빌드 산출물(`bin/...`)과 소스(`src/...`) 동시 수정 위험이 있어 작업 기준 정리가 필요하다.

## 4) 우선순위 로드맵

## P0 (긴급, 이번 사이클 최우선)
### A. 정보구조(IA)와 글로벌 네비게이션 정비
- 목적: 사용자가 3클릭 이내로 원하는 기능에 도달하도록 메뉴 구조 단순화
- 수정 대상
  - `src/main/resources/templates/fragments/header.html`
  - `src/main/resources/templates/fragments/footer.html`
  - `src/main/resources/templates/layouts/default_layout.html`
  - `src/main/resources/static/css/common.css`
- 산출물
  - 확정 메뉴 트리(1-depth/2-depth)
  - 공통 헤더/푸터 컴포넌트 개선안
  - 모바일 우선 네비게이션 동작 정의(접기/펼치기/검색 진입)

### B. 메인/검색 경험 개선 (전환율 개선 핵심)
- 목적: 메인에서 핵심 기능 유입률 상승, 검색 결과 가독성/정확도 개선
- 수정 대상
  - `src/main/resources/templates/views/main/index.html`
  - `src/main/resources/templates/views/main/search.html`
  - `src/main/java/com/codemoa/project/domain/main/controller/MainController.java`
  - `src/main/resources/static/css/main.css`
  - `src/main/resources/static/css/common.css`
- 산출물
  - 메인 섹션 우선순위 재정의(히어로, 커뮤니티 최신글, 추천 강의)
  - 검색 결과 카드/필터/빈결과 UX 개선안
  - 메인 노출 데이터 기준(정렬/개수/빈 데이터 처리)

### C. URL/라우팅 일관성 정책 수립
- 목적: 경로 체계를 통일해 사용자 인지와 개발 생산성 동시 개선
- 우선 점검 파일
  - `src/main/java/com/codemoa/project/domain/recruit/controller/TeamRecruitController.java`
  - `src/main/java/com/codemoa/project/domain/community/controller/BoardViewController.java`
  - `src/main/java/com/codemoa/project/domain/information/controller/InformationController.java`
  - `src/main/java/com/codemoa/project/domain/problem/controller/ProblemController.java`
- 산출물
  - URL 규칙 문서(소문자-하이픈, 리소스 기반, 목록/상세/작성/수정 표준)
  - 리다이렉트/하위호환 정책 초안(기존 링크 깨짐 방지)

## P1 (중요, P0 완료 직후)
### D. 도메인별 화면 품질 표준화
- 목적: 페이지마다 다른 레이아웃 밀도/폼/버튼/테이블 사용성을 통일
- 수정 대상(대표)
  - 커뮤니티:  
    - `src/main/resources/templates/views/community/boardList.html`
    - `src/main/resources/templates/views/community/boardDetail.html`
    - `src/main/resources/templates/views/community/boardWrite.html`
    - `src/main/resources/templates/views/community/boardUpdate.html`
  - 문제풀이:
    - `src/main/resources/templates/views/problem/problemList.html`
    - `src/main/resources/templates/views/problem/problemDetail.html`
    - `src/main/resources/templates/views/problem/problemWriteForm.html`
    - `src/main/resources/templates/views/problem/problemUpdateForm.html`
  - 정보/채용/팀:
    - `src/main/resources/templates/views/information/*.html`
    - `src/main/resources/templates/views/employment/employmentList.html`
    - `src/main/resources/templates/views/recruit/teamRecruitList.html`
    - `src/main/resources/templates/views/recruit/teamRecruitDetail.html`
  - 공통/페이지별 스타일:
    - `src/main/resources/static/css/common.css`
    - `src/main/resources/static/css/problem.css`
    - `src/main/resources/static/css/information/*.css`
    - `src/main/resources/static/css/mypage/*.css`
- 산출물
  - 공통 컴포넌트 사용 가이드(카드, 버튼, 폼, 테이블, 뱃지)
  - 페이지 템플릿 점검 체크리스트

### E. 기능 신뢰성/보안 UX 강화
- 목적: 권한/예외/실패 케이스에서 사용자 혼란 최소화
- 수정 대상
  - `src/main/java/com/codemoa/project/configurations/SecurityConfig.java`
  - `src/main/java/com/codemoa/project/domain/exception/GlobalExceptionHandler.java`
  - (신규 필요 가능) `src/main/resources/templates/error/*.html`
- 산출물
  - 인증 필요 페이지 접근 시 UX 규칙(로그인 유도/리턴 URL)
  - 공통 오류 응답 + 사용자용 오류 페이지 정책

## P2 (개선/확장, 안정화 단계)
### F. 마이페이지/관리자/채팅 고도화
- 수정 대상(대표)
  - `src/main/resources/templates/views/user/mypage/*.html`
  - `src/main/resources/templates/views/user/admin/*.html`
  - `src/main/resources/templates/views/chat/*.html`
  - `src/main/resources/static/js/mypage/*.js`
  - `src/main/resources/static/js/admin/users.js`
  - `src/main/java/com/codemoa/project/domain/user/controller/MyPageController.java`
  - `src/main/java/com/codemoa/project/domain/user/controller/AdminController.java`
  - `src/main/java/com/codemoa/project/domain/chat/controller/*.java`
- 산출물
  - 대시보드형 정보 배치 개선안
  - 관리자 운영 플로우 개선안(검색/필터/상세 액션)

### G. 운영 기준 정리 (품질/배포)
- 목적: 소스/산출물 혼선 방지, 회귀 리스크 축소
- 수정/관리 대상
  - `README.md`
  - `.gitignore`
  - (검토) `bin/main/templates/views/main/index.html` 관리 정책
- 산출물
  - "소스 우선 수정 경로" 규칙 문서화
  - 배포 전 체크리스트(화면, 권한, 링크, 폼 검증)

## 5) 실행 순서 (권장)
- 1주차: P0-A, P0-B (디자인/탐색/메인 전환)
- 2주차: P0-C, P1-D 일부 (라우팅 정책 + 핵심 도메인 화면 표준화)
- 3주차: P1-E, P2-F 일부 (오류/보안 UX + 운영 기능)
- 4주차: P2-G 및 전체 회귀 점검

## 6) 완료 기준 (Definition of Done)
- 주요 사용자 여정(메인→탐색→상세→작성)이 끊김 없이 동작
- 모바일(최소 375px)과 데스크톱(1280px+)에서 핵심 화면 레이아웃 깨짐 없음
- URL/네이밍 규칙 문서와 실제 라우팅이 일치
- 인증/권한/예외 케이스에서 사용자 안내가 명확함
- 기획 문서 기준으로 QA 체크리스트 통과

## 7) 즉시 착수 추천 Top 5 파일
- `src/main/resources/templates/fragments/header.html`
- `src/main/resources/templates/views/main/index.html`
- `src/main/resources/templates/views/main/search.html`
- `src/main/resources/static/css/common.css`
- `src/main/java/com/codemoa/project/domain/recruit/controller/TeamRecruitController.java`


---

## Part B. 메인 페이지 기획 (상세)

## 개요

현재 CodeMoa 메인 페이지(`/`)는 10개 이상의 섹션이 수직 나열된 "정보 나열형" 구조다.
데이터는 풍부하지만 **사용자가 목적지에 빠르게 도달하기 어렵고**, 로그인 여부와 관계없이 거의 동일한 화면이 노출된다.
이 기획서는 **목적별 사용자 여정**을 기준으로 섹션 우선순위를 재정의하고, 로그인 사용자에게 개인화 경험을 제공하며, ElevenLabs 디자인 시스템(`DESIGN.md`) 기준으로 시각 일관성을 확보하는 방향으로 메인 페이지를 개선한다.

---

## 사용자 스토리

- **처음 방문한 비로그인 사용자**로서, CodeMoa가 어떤 서비스인지 5초 이내에 파악하고 회원가입·탐색 행동을 하고 싶다.
- **재방문 로그인 사용자**로서, 내가 놓친 활동(미답변 Q&A, 마감 임박 공모전, 새 커뮤니티 글)을 한눈에 파악하고 싶다.
- **AI 문제 풀이 목적 사용자**로서, 메인에서 오늘 풀어야 할 문제로 바로 진입하고 싶다.
- **취업/팀 모집 목적 사용자**로서, 현재 올라와 있는 공고·모집 글 상태를 빠르게 스캔하고 싶다.

---

## 현재 구조의 핵심 문제

| 문제 영역 | 현재 상태 | 영향 |
|-----------|-----------|------|
| 섹션 우선순위 불명확 | 10개+ 섹션이 동일 비중으로 나열됨 | 사용자가 무엇부터 볼지 결정 불가 |
| 개인화 부재 | 히어로 외 로그인/비로그인 동일 화면 | 재방문 사용자 재참여 동기 약함 |
| 게시판별 최신 섹션 | 5컬럼 가로 나열 | 모바일에서 스캔 불가, 정보 밀도 낮음 |
| 섹션 간 시각 구분 약함 | 유사한 카드 스타일 반복 | 스크롤 피로, 섹션 인지 어려움 |
| 빈 상태 데모 데이터 | DB 미연결 시 더미 데이터 노출 | 실데이터 착각 가능, 신뢰도 저하 |
| 오늘의 AI 문제 위치 | 통계 바·칩 트레이 아래 묻힘 | 핵심 기능 진입점이 눈에 안 띔 |
| 디자인 시스템 미적용 | 메인 전용 스타일이 공통 토큰과 혼재 | 브랜드 일관성 약함 |

---

## 화면 목록

| URL | 화면명 | 설명 |
|-----|--------|------|
| `/` (비로그인) | 메인 — 랜딩형 | 서비스 소개 + 핵심 기능 진입 유도 |
| `/` (로그인) | 메인 — 개인화 대시보드형 | 내 활동 요약 + 맞춤 콘텐츠 노출 |

> 두 화면은 같은 URL이며, Thymeleaf `sec:authorize`로 섹션을 분기한다.

---

## 기능 상세

### 기능 1: 히어로 섹션 — 비로그인 랜딩 강화

현재 히어로는 카피 문구 + CTA 2개로 충분하지만, **서비스 가치를 즉각 전달하는 요소**가 부족하다.

- 수락 조건:
  - [ ] 헤드라인 1줄, 서브 텍스트 1줄, CTA Primary 1개(`AI 문제 풀기`), CTA Secondary 1개(`커뮤니티`) 구조 유지
  - [ ] 히어로 하단에 **3가지 핵심 가치**를 아이콘+텍스트로 한 줄 나열 (예: "AI 피드백", "개발자 커뮤니티", "채용 정보")
  - [ ] 회원가입 링크를 CTA 근처에 텍스트 링크로 추가 ("아직 계정이 없으신가요? 가입하기")
  - [ ] 통합 검색창은 히어로 내 중요도를 낮추거나 헤더로 이전 (별도 문서로 분리 가능)
  - [ ] CTA Primary는 검정 배경·흰 글씨·완전 둥근 알약형, CTA Secondary는 웜스톤(연한 베이지) 배경·검정 글씨 알약형으로 표시되어야 함

---

### 기능 2: 로그인 사용자 개인화 요약 카드

현재 로그인 히어로는 닉네임·등급·포인트를 보여주지만, **"지금 내가 해야 할 것"을 안내하지 않는다**.

- 수락 조건:
  - [ ] 로그인 시 히어로 아래에 **"내 활동 요약" 카드** 1줄 노출 (최대 3개 항목)
  - [ ] 항목 후보 (우선순위 순):
    1. 마지막으로 시도한 문제가 있을 경우 → "미완료 문제: [제목] 이어 풀기" 링크
    2. 이번 주 푼 문제가 0개일 경우 → "이번 주 첫 문제 도전하기" CTA
    3. 내 Q&A에 새 답변이 달렸을 경우 → "내 질문에 답변이 달렸습니다" 링크
  - [ ] 데이터가 없는 항목은 노출하지 않음 (항목이 0개면 카드 자체를 숨김)
  - [ ] 이 카드는 서버 렌더링 (JS 없이 Thymeleaf 조건부 출력)

---

### 기능 3: 섹션 순서 재정의

현재 섹션 순서를 **사용자 행동 빈도 및 전환 중요도** 기준으로 재배치한다.

**개선 전 순서:**
히어로 → 통계 바 → 칩 트레이 → 오늘의 AI 문제 → 핵심 허브(4개 아이콘) → 커뮤니티 피드+사이드 → 게시판별 최신 → IT 정보 허브 → 프로젝트 다이어리 → 채용·팀 → FAQ

**개선 후 순서:**

| 순서 | 섹션명 | 변경 사항 |
|------|--------|-----------|
| 1 | 히어로 | 유지 (기능 1 적용) |
| 2 | 개인화 요약 카드 | 신규 (로그인 시만) |
| 3 | 통계 바 | 유지 |
| 4 | 오늘의 AI 문제 | **위로 이동** (칩 트레이 앞으로) |
| 5 | 칩 트레이 | 기존 위치보다 아래로 |
| 6 | 커뮤니티 피드 + 사이드바 | 유지 |
| 7 | IT 정보 허브 (강좌·도서·공모전) | 유지 |
| 8 | 채용·팀 모집 | 유지 |
| 9 | 게시판별 최신 | **아래로 이동** (탭형 개선 후) |
| 10 | 프로젝트 다이어리 | 유지 (위치 검토) |
| 11 | FAQ | 유지 |
| (제거) | 핵심 허브 4아이콘 그리드 | 칩 트레이와 역할 중복 → 제거 |

- 수락 조건:
  - [ ] "핵심 허브" 4아이콘 섹션은 칩 트레이와 중복이므로 **제거**
  - [ ] "오늘의 AI 문제" 위젯이 히어로·개인화 요약 카드 다음으로 노출됨
  - [ ] 섹션 간 시각적 구분(배경색 교차 또는 구분선)이 적용됨

---

### 기능 4: 게시판별 최신 — 탭형 UX로 전환

현재 5컬럼 가로 카드는 데스크톱에서도 정보 밀도가 낮고, 모바일에서는 세로 스택으로 뭉개진다.

- 수락 조건:
  - [ ] 게시판 탭(자유 / Java / JavaScript / Python / Kotlin)을 상단에 배치
  - [ ] 탭 전환 시 해당 게시판 최신 글 **최대 8건** 리스트로 표시
  - [ ] 각 리스트 아이템은 **제목 + 날짜 + 추천수** 3가지 메타 표시
  - [ ] 현재 탭에서 "더보기" 클릭 시 해당 게시판으로 이동
  - [ ] 모바일에서 탭이 가로 스크롤 가능해야 함

---

### 기능 5: 빈 상태(Empty State) 정책 정비

현재 DB가 비어있으면 더미 데이터를 채운다. 이는 **실제 데이터처럼 오인**될 수 있다.

- 수락 조건:
  - [ ] 더미 데이터를 사용하는 섹션(랭킹, 강좌, 도서, 공모전, 채용, 팀 모집)에 "샘플" 표시 또는 **더미 데이터 제거** 후 빈 상태 UX로 대체 (개발자 협의 후 결정)
  - [ ] 빈 상태 UX 기준: 아이콘 1개 + 안내 문구 1줄 + 행동 유도 링크(CTA) 1개
  - [ ] `mainShowDemoNotice` 배너는 커뮤니티 글 없을 때만 표시, 위치를 해당 섹션 안으로 이동

---

### 기능 6: Q&A 하이라이트 개선

현재 Q&A 하이라이트는 단순 제목 목록이다. **"지금 답해줄 수 있는 질문"** 관점으로 개선한다.

- 수락 조건:
  - [ ] 미답변 Q&A를 우선 노출 (답변완료 글은 하단 배치 또는 제외)
  - [ ] 각 Q&A 아이템에 **경과 시간** 표시 (예: "3시간 전", "2일 전")
  - [ ] "미답변 [n]건" 뱃지를 섹션 헤더에 표시
  - [ ] 리스트 하단에 "질문하기" CTA 링크 추가

---

### 기능 7: 통계 바 확장 (선택)

현재 4개 항목에 이번 주 활동 지표를 추가해 서비스의 활발함을 전달한다.

- 수락 조건:
  - [ ] 기존 4개 항목(총 회원, 등록 문제, 총 게시글, 공모전) 유지
  - [ ] 추가 후보 1개: **"이번 주 활동자 n명"** 또는 **"오늘 새 글 n건"** (데이터 가용 시)
  - [ ] 모바일에서 항목이 2×2 그리드 또는 가로 스크롤로 표시되어야 함

---

### 기능 8: 디자인 시스템 적합성 — 메인 페이지 전용

메인 페이지의 시각 요소를 `DESIGN.md`(ElevenLabs 디자인 시스템) 기준에 맞춰 통일한다.
구체적인 색상·수치는 `DESIGN.md`를 참고하며, 아래는 **사용자가 체감할 수 있는 요구사항**이다.

- 수락 조건:
  - [ ] 메인 페이지 색상은 화이트, 라이트 그레이, 웜스톤(연한 베이지), 블랙 계열만 사용 — 기존 브랜드 틸/액센트 색은 메인에서 미노출
  - [ ] 섹션 제목은 얇은(light) 두께의 폰트로, 굵게 처리하지 않음
  - [ ] 카드/패널은 흰 배경, 둥근 모서리, 그림자 3겹 스택 형태로 통일
  - [ ] 채용·팀 모집 섹션은 웜스톤 배경의 독립 구역으로 시각적으로 구분됨
  - [ ] 모든 버튼·칩의 터치 영역은 세로 44px 이상 확보 (모바일 접근성)
  - [ ] 키보드 포커스 시 각 링크·버튼에 연한 파란색 포커스 링이 표시됨
  - [ ] 리스트 행 높이는 최소 48px 이상 유지 (터치 친화적)

---

## 시나리오

### 정상 케이스

**A. 비로그인 첫 방문:**
1. `/` 접속 → 히어로(카피 + CTA 2개 + 서비스 가치 3줄) 노출
2. 통계 바로 서비스 규모 확인
3. 오늘의 AI 문제 위젯으로 핵심 기능 체험 유도
4. 커뮤니티 피드에서 최신 글 훑어보기
5. IT 정보 허브에서 강좌/공모전 탐색
6. 채용·팀 섹션에서 공고 확인
7. FAQ로 서비스 이해 → 가입 결정

**B. 로그인 재방문:**
1. `/` 접속 → 히어로(닉네임·등급·포인트) + **개인화 요약 카드** 노출
2. "미완료 문제 이어 풀기" 또는 "이번 주 첫 문제" CTA 클릭 → 문제 페이지 이동
3. 커뮤니티 피드 인기/최신 탭으로 관심 글 탐색
4. 사이드바 Q&A 하이라이트에서 미답변 질문에 답변 참여

### 예외 케이스

1. **DB 전체 비어 있음**: 각 섹션이 "빈 상태 UX"를 노출하고, 관리자만 볼 수 있는 데모 배너로 구분
2. **오늘의 AI 문제 없음**: 위젯 자체를 숨김
3. **개인화 요약 카드 데이터 없음**: 카드 자체 숨김, 히어로 바로 아래 통계 바로 넘어감
4. **모바일(375px) 접속**: 5컬럼 게시판 섹션이 탭형으로 표시, 통계 바 항목이 2×2로 재배열
5. **로그인 세션 만료 상태**: 개인화 카드 미노출 (서버 렌더링이므로 자동 처리)

---

## 데이터 요구사항

| 필드명 | 설명 | 신규 여부 | 필수 |
|--------|------|-----------|------|
| `personalizedActions` | 개인화 요약 카드 항목 목록 (최대 3개) | 신규 | 로그인 시 |
| `lastAttemptedProblem` | 마지막으로 시도한 문제 (제목, ID) | 신규 | 선택 |
| `weeklyProblemCount` | 이번 주 푼 문제 수 | 신규 | 로그인 시 |
| `myUnansweredQnaCount` | 내 Q&A 중 미답변 건수 | 신규 | 로그인 시 |
| `statsActiveUsersThisWeek` | 이번 주 활동 사용자 수 | 신규 | 선택 |
| `highlightQnas` | Q&A 하이라이트 목록 — 경과 시간 필드 추가 | 기존 확장 | 필수 |
| `popularPosts` | 인기 커뮤니티 글 | 기존 유지 | 필수 |
| `weeklyRanking` | 주간 랭킹 TOP 5 | 기존 유지 | 필수 |

> 신규 필드 중 로그인 시 필요한 항목은 비로그인 시 null로 설정한다.

---

## 역할/권한

| 기능 | 비로그인 | 로그인(일반) | 관리자 |
|------|----------|-------------|--------|
| 히어로 CTA (AI 문제) | 클릭 시 로그인 유도 | 문제 페이지 이동 | 동일 |
| 개인화 요약 카드 | 미노출 | 노출 | 노출 |
| 통계 바 | 노출 | 노출 | 노출 |
| 오늘의 AI 문제 | 노출 (클릭 시 로그인 유도) | 노출 | 노출 |
| 커뮤니티 피드 | 읽기 전용 노출 | 노출 | 노출 |
| 게시판 탭 | 읽기 전용 노출 | 노출 | 노출 |
| Q&A "질문하기" CTA | 클릭 시 로그인 유도 | 질문 작성 페이지 | 동일 |
| 빈 상태 "등록하기" CTA | 미노출 | 미노출 | 등록 페이지 |

---

## 구현 우선순위 (롤아웃 순서)

| 단계 | 항목 | 난이도 |
|------|------|--------|
| 1단계 | 섹션 순서 재배치 + 핵심 허브 4아이콘 제거 | 낮음 |
| 1단계 | 오늘의 AI 문제 위젯 위치 이동 | 낮음 |
| 1단계 | 디자인 시스템 색·타이포·카드 통일 | 중간 |
| 2단계 | 게시판별 최신 → 탭형 전환 | 중간 |
| 2단계 | Q&A 하이라이트 미답변 우선 정렬 + 경과 시간 표시 | 중간 |
| 3단계 | 개인화 요약 카드 (신규 쿼리 필요) | 높음 |
| 3단계 | 빈 상태 UX 정비 + 데모 데이터 제거 여부 결정 | 중간 |
| 4단계 | 통계 바 이번 주 활동자 수 추가 | 중간 |
| 4단계 | 히어로 가치 제안 3줄 + 가입 링크 추가 | 낮음 |

---

## 관련 파일 참조

| 역할 | 경로 |
|------|------|
| 메인 템플릿 | `src/main/resources/templates/views/main/index.html` |
| 메인 컨트롤러 | `src/main/java/com/codemoa/project/domain/main/controller/MainController.java` |
| 메인 전용 CSS | `src/main/resources/static/css/main.css` |
| 공통 토큰 | `src/main/resources/static/css/site-ds.css` |
| 더미 데이터 클래스 | `src/main/java/com/codemoa/project/domain/main/support/MainPageDemoData.java` |
| 레이아웃 | `src/main/resources/templates/layouts/default_layout.html` |
| 디자인 시스템 참고 | `docs/DESIGN.md` |

---

## Part C. AI 코딩 문제 풀이 기획 (상세)

## 개요

현재 AI 코딩 문제 풀이 기능(`/problems`)은 **문제 목록 → 답안 입력 → AI 채점 결과** 흐름의 기본 뼈대는 갖추었으나,
풀이 이력이 저장되지 않고, 코드 입력 환경이 일반 텍스트박스 수준이며, 결과 페이지 UI가 미완성(`임시 css 사용중`)인 상태다.
이 기획서는 **사용자가 문제를 반복해서 풀고, 성장을 체감하며, AI를 자연스럽게 학습 도구로 활용**할 수 있도록 기능 추가와 고도화 방향을 정의한다.

---

## 사용자 스토리

- **취업 준비 대학생**으로서, 내가 과거에 푼 문제와 받은 점수를 확인하고 성장을 느끼고 싶다.
- **코딩 학습자**로서, 문제를 틀렸을 때 어디가 부족했는지 AI에게 구체적인 피드백을 받고 싶다.
- **반복 학습자**로서, 틀린 문제만 모아서 다시 풀고 싶다.
- **AI 활용 학습자**로서, 문제를 풀다가 막히면 코드 작성 화면을 벗어나지 않고 AI에게 바로 질문하고 싶다.
- **목표 지향 학습자**로서, 내가 어떤 언어와 난이도를 많이 틀리는지 통계로 보고 싶다.

---

## 현재 구조의 핵심 문제

| 문제 영역 | 현재 상태 | 사용자 불편 |
|-----------|-----------|-------------|
| 풀이 이력 없음 | 제출 후 저장 안 됨 | 지난 점수·피드백을 다시 볼 수 없음 |
| 코드 입력 환경 열악 | 일반 textarea | 탭 키, 들여쓰기, 문법 색상 없음 |
| 결과 페이지 UI 미완성 | 임시 CSS, 구조 없음 | 점수·피드백 가독성 매우 낮음 |
| AI 질문이 문제와 분리 | `/problems/aiAnswer` 별도 페이지 | 문제 풀다 AI 질문 시 화면 이탈 필요 |
| 푼 문제 구분 없음 | 목록에서 풀었는지 알 수 없음 | 안 푼 문제를 찾기 위해 전부 클릭해야 함 |
| Kotlin 언어 없음 | JAVA·JS·Python 3개만 | 커뮤니티 Kotlin 게시판과 불일치 |
| 포인트 지급 기준 불명확 | 제출 시 포인트 지급 로직 미구현 | 포인트 시스템 동기 부여 기능 사라짐 |
| 페이지네이션·검색 없음 | 문제 수 증가 시 전체 나열 | 문제가 많아지면 탐색 불가 |

---

## 화면 목록

| URL | 화면명 | 설명 |
|-----|--------|------|
| `/problems/` | 문제 목록 | 필터·검색·풀이 상태 표시 |
| `/problems/problemDetail?problemId={id}` | 문제 풀기 | 코드 입력 + 인라인 AI 질문 |
| `/problems/problemResult` | AI 채점 결과 | 점수·피드백·다시 풀기 |
| `/problems/myHistory` | 내 풀이 이력 | 과거 제출 목록·점수 |
| `/problems/stats` | 내 학습 통계 | 언어별·난이도별 정답률 |
| `/problems/aiAnswer` | AI 자유 질문 | 문제와 무관한 코딩 질문 (기존 유지) |
| `/problems/problemWrite` | 문제 작성 (관리자) | 기존 유지 + 미리보기 추가 |

---

## 기능 상세

### 기능 1: 풀이 이력 저장

현재 제출 후 결과가 저장되지 않아, 과거 피드백을 다시 볼 수 없고 다시 풀기도 불가능하다.

- 수락 조건:
  - [ ] 로그인 사용자가 문제를 제출하면 **제출 이력 1건**이 저장됨
  - [ ] 저장 항목: 사용자 ID, 문제 ID, 제출 답안, AI 점수(0~100), 제출 일시
  - [ ] 같은 문제를 여러 번 제출할 수 있으며, **각 제출마다 이력이 누적**됨 (덮어쓰기 금지)
  - [ ] 비로그인 사용자는 제출 시 로그인 유도 안내를 표시하고 제출을 차단함
  - [ ] 저장된 이력은 "내 풀이 이력" 페이지에서 확인 가능

---

### 기능 2: 문제 목록 — 풀이 상태 및 탐색성 개선

현재 목록은 단순 나열이며, 내가 풀었는지 여부와 점수를 알 수 없다.

- 수락 조건:
  - [ ] 로그인 사용자에게 각 문제 아이템에 **풀이 상태 배지** 표시
    - 미시도: 표시 없음 (기본)
    - 풀었음 + 최고 점수: 예) ✓ `82점` 뱃지
  - [ ] 문제 제목으로 **키워드 검색** 가능 (입력 즉시 클라이언트 측 필터링)
  - [ ] 기존 언어 필터(JAVA·JS·Python)에 **Kotlin** 추가
  - [ ] 문제가 20개 초과 시 **페이지네이션** 또는 무한 스크롤 적용
  - [ ] 목록 상단에 **내 진행 요약** 1줄 표시: "전체 n개 중 m개 풀었습니다 (정답률 k%)"
    → 비로그인 시 미표시

---

### 기능 3: 문제 풀기 화면 — 코드 입력 환경 개선

현재 답안 입력이 일반 textarea여서 코딩 경험이 나쁘다.

- 수락 조건:
  - [ ] 코드 입력 영역에서 **Tab 키가 들여쓰기(스페이스 4칸)로 동작** (현재 포커스 이탈)
  - [ ] 코드 입력 영역은 **줄 번호** 가 표시되어야 함
  - [ ] 언어 선택 드롭다운이 코드 입력 영역 상단에 표시되어, 선택한 언어에 맞게 힌트 텍스트가 바뀜 (예: "Java 코드를 입력하세요")
  - [ ] 문제 내용 패널과 코드 입력 패널의 **세로 높이를 브라우저 뷰포트에 맞게 고정** (스크롤이 카드 내부에서 발생)
  - [ ] 모바일(768px 이하)에서 문제 내용 패널이 **접기/펼치기** 가능

---

### 기능 4: 문제 풀기 화면 — 인라인 AI 질문 패널

현재 AI 질문을 하려면 `/problems/aiAnswer`로 이동해야 해서 작성 중인 코드가 사라진다.

- 수락 조건:
  - [ ] 문제 풀기 화면 우측 하단에 **"AI에게 질문"** 버튼 상시 표시
  - [ ] 버튼 클릭 시 화면 하단에서 **슬라이드업 패널** 또는 **모달**로 AI 질문 입력창 열림
  - [ ] 질문 전송 시, 현재 문제의 제목·언어 정보가 AI 질문 컨텍스트에 자동 포함됨
  - [ ] AI 답변은 패널 안에서 표시되며, 패널을 닫아도 **코드 입력 내용이 유지**됨
  - [ ] 패널 내 AI 질문은 세션 동안 최대 5회까지 허용 (남은 횟수 표시)

---

### 기능 5: AI 채점 결과 페이지 재설계

현재 결과 페이지는 UI가 "임시 CSS" 상태로 미완성이다.

- 수락 조건:
  - [ ] 페이지 상단에 **점수를 크게 표시** (예: `82 / 100`)
  - [ ] 점수 아래에 **등급 표현** 표시: 90점↑ 우수, 70점↑ 양호, 50점↑ 보통, 50점↓ 재도전
  - [ ] 문제 내용 / 제출 답안 / AI 피드백 3개 영역을 **탭** 또는 **아코디언**으로 구분
  - [ ] 모범 답안은 **기본 숨김 상태**, "모범 답안 보기" 버튼 클릭 시 표시
  - [ ] 하단 버튼: "다시 풀기"(문제 페이지로 이동) / "다른 문제 풀기"(목록) / "내 이력 보기"
  - [ ] AI 피드백 텍스트에서 **코드 블록(`\`\`\`...`)이 구분되어 표시**됨

---

### 기능 6: 내 풀이 이력 페이지 (신규)

과거에 푼 문제와 점수를 한눈에 볼 수 있는 개인 이력 페이지를 제공한다.

- 수락 조건:
  - [ ] 로그인 사용자만 접근 가능; 비로그인 시 로그인 페이지로 리다이렉트
  - [ ] 내가 제출한 이력을 **최신 순**으로 목록 표시
  - [ ] 각 이력 아이템: 문제 제목, 언어, 난이도, 점수, 제출 일시
  - [ ] 같은 문제를 여러 번 푼 경우 **최고 점수**를 강조 표시
  - [ ] 점수 필터: 전체 / 90점↑ / 50점↓ (재도전 필요)
  - [ ] 목록에서 해당 문제로 바로 이동하는 "다시 풀기" 링크 제공
  - [ ] 이력이 없으면 "아직 푼 문제가 없습니다. 첫 문제 도전하기" 빈 상태 표시

---

### 기능 7: 내 학습 통계 페이지 (신규)

언어별·난이도별 약점을 시각적으로 파악할 수 있는 통계 화면을 제공한다.

- 수락 조건:
  - [ ] 로그인 사용자만 접근 가능
  - [ ] **총 제출 수 / 평균 점수 / 최고 점수** 숫자 3개를 상단 카드로 표시
  - [ ] **언어별 평균 점수**: JAVA / JavaScript / Python / Kotlin 각각의 평균 (막대 또는 수치)
  - [ ] **난이도별 정답률**: 상·중·하 각각 "시도 n회 중 m회 70점↑"로 표시
  - [ ] **최근 7일 제출 수** 표시 (연속 학습 현황)
  - [ ] 통계 데이터가 없으면 (이력 0건) 빈 상태 + "문제 풀러 가기" CTA 표시

---

### 기능 8: 포인트 지급 정책 연동

현재 문제 제출 시 포인트 지급 로직이 없어 포인트 시스템 동기 부여 효과가 없다.

- 수락 조건:
  - [ ] 문제 제출 후 AI 점수에 따라 **포인트 자동 지급**
    - 90점 이상: 100P
    - 70~89점: 60P
    - 50~69점: 30P
    - 50점 미만: 10P (도전 자체에 보상)
  - [ ] 같은 문제는 **하루 1회만 포인트 지급** (반복 제출로 포인트 남용 방지)
  - [ ] 결과 페이지에서 **"+ n 포인트 획득"** 메시지 표시
  - [ ] 포인트 지급 여부(오늘 이미 받음)도 결과 페이지에서 안내

---

### 기능 9: 문제 작성 폼 개선 (관리자)

관리자가 문제를 작성할 때 실제 표시 형태를 미리 확인할 수 없다.

- 수락 조건:
  - [ ] 문제 내용 입력 영역 우측 또는 하단에 **실시간 미리보기** 패널 제공
  - [ ] 힌트 필드에 **"없음"** 기본값 또는 선택 입력 안내 표시 (현재 빈 칸이면 힌트 버튼이 빈 내용을 표시)
  - [ ] 언어(category) 선택을 자유 입력 → **드롭다운 선택**으로 변경 (JAVA·JavaScript·Python·Kotlin)
  - [ ] 난이도(difficulty) 선택을 자유 입력 → **드롭다운 선택**으로 변경 (상·중·하)
  - [ ] 작성 완료 후 **"작성한 문제 바로 보기"** 링크 제공

---

## 시나리오

### 정상 케이스

**A. 처음 방문 → 문제 풀기 → 결과 확인:**
1. `/problems/` 접속 → 언어·난이도 필터로 원하는 문제 탐색
2. 문제 아이템 클릭 → 문제 풀기 화면 진입
3. 코드 입력 → 막히면 "AI에게 질문" 패널 열기 → 힌트 얻음 → 패널 닫기 → 코드 계속 작성
4. 제출 클릭 → 결과 페이지로 이동 → 점수·피드백 확인
5. 포인트 획득 메시지 확인
6. "다시 풀기" 또는 "다른 문제 풀기" 선택

**B. 재방문 → 약점 파악 → 재도전:**
1. `/problems/stats` 접속 → JavaScript 평균 점수가 낮음을 확인
2. `/problems/` 에서 JavaScript 필터 선택 → 목록에서 낮은 점수 문제 찾기
3. `/problems/myHistory` 에서 50점 이하 필터 → "다시 풀기" 링크 클릭
4. 재도전 후 점수 향상 확인

### 예외 케이스

1. **비로그인 제출 시도**: 제출 버튼 클릭 시 "로그인이 필요합니다" 안내 + 로그인 페이지 링크
2. **AI 서버 응답 지연/실패**: 결과 페이지에서 "AI 피드백을 가져오는 중입니다" → 30초 초과 시 "잠시 후 다시 시도해 주세요" 안내; 점수는 0점으로 저장하지 않고 이력 저장 보류
3. **빈 코드 제출**: 제출 전 클라이언트 검증 — 빈 입력 시 "답안을 입력해 주세요" 오류 메시지 표시
4. **문제가 없는 경우**: 필터 조합 결과 0건일 때 "해당 조건의 문제가 없습니다. 필터를 초기화하세요" 안내
5. **하루 포인트 중복 지급 시도**: 결과 페이지에서 "오늘 이미 이 문제로 포인트를 받으셨습니다" 안내, 포인트 미지급
6. **인라인 AI 질문 5회 초과**: "오늘 AI 질문 횟수를 모두 사용했습니다. 내일 다시 이용해 주세요" 안내

---

## 데이터 요구사항

| 필드명 | 설명 | 신규 여부 | 필수 |
|--------|------|-----------|------|
| `submissionId` | 제출 이력 PK | 신규 | 필수 |
| `userId` | 제출 사용자 ID | 신규 | 필수 |
| `problemId` | 제출한 문제 ID | 신규 | 필수 |
| `submittedAnswer` | 사용자 제출 코드 | 신규 | 필수 |
| `aiScore` | AI 채점 점수 (0~100) | 신규 | 필수 |
| `aiFeedback` | AI 피드백 전문 | 신규 | 선택 |
| `submittedAt` | 제출 일시 | 신규 | 필수 |
| `pointAwarded` | 이 제출로 지급한 포인트 (0이면 미지급) | 신규 | 필수 |
| `problem.category` | 문제 언어 (JAVA·JavaScript·Python·Kotlin) | 기존 확장 (Kotlin 추가) | 필수 |
| `myBestScore` | 해당 문제의 내 최고 점수 (목록 표시용) | 신규 (집계) | 로그인 시 |

---

## 역할/권한

| 기능 | 비로그인 | 로그인(일반) | 관리자 |
|------|----------|-------------|--------|
| 문제 목록 조회 | 가능 | 가능 (풀이 상태 표시) | 가능 |
| 문제 상세 조회 | 가능 | 가능 | 가능 |
| 답안 제출 | 로그인 유도 차단 | 가능 | 가능 |
| AI 채점 결과 조회 | 불가 | 가능 | 가능 |
| 인라인 AI 질문 | 로그인 유도 | 가능 (일 5회) | 가능 (횟수 제한 없음) |
| 내 풀이 이력 | 불가 (리다이렉트) | 가능 | 가능 |
| 내 학습 통계 | 불가 (리다이렉트) | 가능 | 가능 |
| 문제 작성·수정·삭제 | 불가 | 불가 | 가능 |
| AI 자유 질문 (`/problems/aiAnswer`) | 가능 | 가능 | 가능 |

---

## 구현 우선순위 (롤아웃 순서)

| 단계 | 항목 | 난이도 | 사용자 체감 |
|------|------|--------|-------------|
| 1단계 | 결과 페이지 UI 재설계 (기능 5) | 낮음 | 높음 |
| 1단계 | 코드 입력 탭 키 동작 + 줄 번호 (기능 3) | 낮음 | 높음 |
| 1단계 | 문제 작성 폼 드롭다운·미리보기 (기능 9) | 낮음 | 중간 |
| 2단계 | 풀이 이력 저장 (기능 1) | 중간 | 높음 |
| 2단계 | 문제 목록 풀이 상태 배지 + Kotlin 추가 (기능 2) | 중간 | 높음 |
| 2단계 | 포인트 지급 연동 (기능 8) | 중간 | 중간 |
| 3단계 | 내 풀이 이력 페이지 (기능 6) | 중간 | 높음 |
| 3단계 | 인라인 AI 질문 패널 (기능 4) | 높음 | 높음 |
| 4단계 | 내 학습 통계 페이지 (기능 7) | 높음 | 중간 |
| 4단계 | 문제 목록 키워드 검색·페이지네이션 (기능 2 일부) | 중간 | 중간 |

---

## 관련 파일 참조

| 역할 | 경로 |
|------|------|
| 문제 목록 템플릿 | `src/main/resources/templates/views/problem/problemList.html` |
| 문제 상세 템플릿 | `src/main/resources/templates/views/problem/problemDetail.html` |
| 결과 템플릿 | `src/main/resources/templates/views/problem/problemResult.html` |
| AI 질문 템플릿 | `src/main/resources/templates/views/problem/aiAnswer.html` |
| 문제 컨트롤러 | `src/main/java/com/codemoa/project/domain/problem/controller/ProblemController.java` |
| AI 서비스 | `src/main/java/com/codemoa/project/domain/problem/service/AiSupportService.java` |
| 문제 서비스 | `src/main/java/com/codemoa/project/domain/problem/service/ProblemService.java` |
| 문제 엔티티 | `src/main/java/com/codemoa/project/domain/problem/entity/Problem.java` |
| 문제 전용 CSS | `src/main/resources/static/css/problem.css` |
