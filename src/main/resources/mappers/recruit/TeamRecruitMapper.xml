<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codemoa.project.domain.recruit.mapper.TeamRecruitMapper">

<insert id="insertTeamRecruit" parameterType="TeamRecruit">
INSERT INTO recruitbbs(userId, contestTitle, recruitType, recruitPeriod, 
activeityPeriod, totalMembers, remainingMembers, progressType, contact, techStack, 
status, applyGuide, title, content, attachmentUrl, viewCount, regDate, updateDate)

VALUES(#{userId}, #{contestTitle}, #{recruitType}, #{recruitPeriod}, 
#{activityPeriod}, #{totalMembers}, #{remainingMembers}, #{progressType}, #{contact}, #{techStack}, 
#{status}, #{applyGuide}, #{title}, #{content}, #{attachmentUrl}, #{viewCount}, SYSDATE(), SYSDATE() )

</insert>

<select id="getTeamRecruit" resultType="TeamRecruit">
SELECT *
FROM recruitbbs
WHERE recruit_Id = #{recruitId}
</select>

<select id="TeamRecruitList" resultType="TeamRecruit">
SELECT *
FROM recruitbbs
ORDER BY recruit_Id DESC
</select>
</mapper>